#ifndef TOKEN_H
#define TOKEN_H

#include <string>

enum TokenType {INT, VAR_OPE, MAT_OPE};

class Token {
	public:
		Token(string value, TokenType type);
		string value;
		TokenType type;
};

#endif
