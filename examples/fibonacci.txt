/fib
{
	/in def
	#"in : " output
	#in print
	in 2 geq
	{
		in 2 - fib 
		in 1 - fib
		+ # Returns fib(n-1) + fib(n-2)
	}
	{
		1
	}
	if
} def
/i 0 def
10 {
	"fib(" output
	     i output
	      ") = " output
	           i fib print
	/i i 1 + def
} repeat